window.QuickSortHelper=window.QuickSortHelper||{};QuickSortHelper.partition=function(source,left,right){	// 默认取第一个元素为中轴节点	var match=source[left];		// 分别从左向右和从右向左	// 与中轴元素进行对比	// 如果左边小于中轴元素的则跳过，否则停止	// 如果右边大于中轴元素的则跳过，否则停止	// 然后交互停止在左边和右边的元素索引	// 左右分别前进一个位置	// 进行下一次比较	// 中断条件为左右索引相交或相遇	var i=left; // 左边元素索引	var j=right; // 右边元素索引		while(i<j){		do{			i--;  // 开始时就减1，因为左边只需要从第二个元素开始比较		}while(source[i]<match && i<j-1); // 不能超过最大索引				do{			j--;  // 由于right代表的是右边的索引+1,所以此处开始时，就需要减1		}while(source[j]>match && j>i-1); // 不能超过最新索引				// 如果在左边找到了大于中轴的元素		// 在右边找到了小于中轴的元素		// 交换它们的位置		// 保证在中轴左边的元素都小于等于中轴		// 在中轴右边的元素都大于等于中轴			}}QuickSortHelper.swap=function(arr,left,right){	var tp=arr[left];	arr[left]=arr[right];	arr[right]=tp;	}